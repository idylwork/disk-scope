# エージェント・コントリビュータ用ドキュメント

人間・AI いずれの実装でも次を踏襲すること。
AI エージェントは明確な指示がない限り当ファイルに追記しない。

## 命名規則

- 例: `(e) => { ... }` のように、内容が直感的でないレベルまで引数名を短縮しない。

## コードコメント

- 新規機能を追加する際、変数の値や変更の経緯を冗長に書かない。読み手がコードから分かることの繰り返しは避ける。
  意図・前提・非自明な理由など、意味合いが伝わるように書く。
- 機能改修時、基本的にコメントを変更しない。特に変更の経緯はコメントには記載しない。
  大きく齟齬が発生しない限り、コード差分が最小限になることを重視すること。
  新規に関数を追加する場合はTSDocを追加する。
  ```ts
  /**
   * ____を取得する
   * @param arg
   * @returns
   */
  ```

## TypeScript

- まだ再利用の見込みがないうちに早すぎる抽象化をしない。

## CSS

- コンポーネントルートのクラス名は特別な理由がない限り`.root`
- レスポンシブ対応は可能な限りコンテナクエリを活用し、基本はモバイルレイアウト、上書きでデスクトップレイアウトを記述する。
  コンテナクエリは比較演算子を活用する。
- コメントは基本使わず、大きなコンポーネント単位のくくりがある時のみ、コンポーネント名を記載する。

### テスト
- モジュール的に使用する関数はファイル名は`*.test.ts`でテストを書く。
